/*
- Utilizando a solução anterior, adicione as opções else if e else.
*/
package main

import ("fmt")

func main() {
	golang := false

	if (golang) {
		fmt.Print("Go é a melhor linguagem de programação")
	}else {
		fmt.Print("Talvez ela não seja tão boa assim")
	}
}